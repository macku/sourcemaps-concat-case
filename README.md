# sourcemaps-concat-case

1. Run:

```sh
npm install
npm run build
```

2. Navigate to [`source-map-visualizer`](https://sokra.github.io/source-map-visualization/#custom)
3. Drag and drop two files to visualizer from the `dist` directory:

- `output.js`
- `output.js.map`
